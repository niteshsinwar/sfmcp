# === Core (needed for your MCP stdio server) ===
mcp[cli]==1.12.4           # includes mcp.server.fastmcp; Python >=3.11
pydantic==2.11.7
pydantic-settings==2.10.1
python-dotenv==1.1.1
requests==2.32.4           # used by your OAuth flow
simple-salesforce==1.12.6
psutil==7.0.0

# If you use Google’s legacy SDK (ok to keep for now)
#google-generativeai==0.8.5

# === Optional (only if you’ll run an HTTP/SSE server) ===
#fastapi==0.116.1
#uvicorn[standard]==0.35.0
#gunicorn==23.0.0
